joinTable2: aj[`id;
                select from
                    update id: id, sales: sales + 0 from
                        (select from person where sales > 500, age > 50);
                `id xkey (sale)]
